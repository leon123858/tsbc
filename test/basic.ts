import { expect } from 'chai';
import { describe } from 'mocha';

describe('check program can execute', function () {
	it('should be right for correct setting', async () => {
		expect(true).is.true;
	});
});
